# Leave blank in dev — requests use relative /v1/* paths via the Vite proxy (see vite.config.ts)
# Set to full origin URL only for production builds.
VITE_API_BASE_URL=
VITE_APP_NAME=RecruitBot
VITE_ENABLE_MOCK=false
